# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Data — raw and processed docs are large and regenerated by ingest
data/raw/
data/processed/
# Keep predictions dir but only latest.json (snapshots can be large)
data/predictions/snapshot_*.json

# OS
.DS_Store
Thumbs.db
