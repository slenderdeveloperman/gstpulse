# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Node
node_modules/
.vercel/

# Data — regenerated by ingest pipeline, not committed
data/raw/
data/processed/
data/chunks/
# Snapshots are large; only latest.json is committed
data/predictions/snapshot_*.json
# Vector store is committed for Phase 2 Cloudflare Worker access
# (ChromaDB flat files, ~30MB for full corpus)
# Uncomment the line below only if the store grows too large for the repo:
# data/vectors/

# OS
.DS_Store
Thumbs.db
