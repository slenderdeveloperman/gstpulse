# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Node
node_modules/
.vercel/

# Data — regenerated by ingest pipeline, not committed
data/raw/
data/processed/
data/chunks/
# Snapshots are large; only latest.json is committed
data/predictions/snapshot_*.json
# Vector store — regenerated locally, not committed (binary ChromaDB files, ~30MB)
data/vectors/

# Supabase CLI temp files
supabase/.temp/

# Backup files
*.bak

# OS
.DS_Store
Thumbs.db
